class LNumber
    (@num ? 0) ->

class LString
    (@str ? "") ->

class LList
    (@list) ->

class LBoolean 
    (@value) ->

class LFunCall
    (@name, @args ? []) ->

class LVariable
    (@name) ->

exports <<< { LString, LNumber, LList, LBoolean, LFunCall, LVariable }

