class LNumber
    (@num ? 0) ->

class LString
    (@str ? "") ->

class LList
    (@list) ->

class LBoolean 
    (@value) ->

class LFunCall
    (@name, @args ? []) ->

class LVariable
    (@name) ->

class LUndefined
    ->

class LNull
    ->

exports <<< { LString, LNumber, LList, LBoolean, LFunCall, LVariable, LUndefined, LNull }

